<Page x:Class="StockFlow.Visual.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="LoginPage">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="322" Height="446">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Login" 
                   FontSize="28" 
                   FontWeight="SemiBold" 
                   HorizontalAlignment="Center" 
                   Foreground="#333333" Margin="0,37,0,39" Grid.RowSpan="4"/>

        <TextBlock Grid.Row="2" Text="Nome de Usuário" Margin="0,42,0,18" Foreground="#555555" Grid.RowSpan="3"/>

        <TextBox x:Name="TxtUsuario" Grid.Row="4" Height="35" BorderBrush="#AAAAAA" BorderThickness="1" Margin="0,8,0,2" Grid.RowSpan="3" HorizontalAlignment="Left" Width="312"/>

        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0"/>

            <CheckBox Grid.Column="1" VerticalAlignment="Bottom" Margin="7,0,3,-80"/>
        </Grid>

        <Button Grid.Row="10"
                Content="Entrar" 
                Height="40" 
                Background="#99CCFF" 
                BorderBrush="#66B2FF" 
                BorderThickness="1"
                Foreground="#333333"
                FontSize="16"
                FontWeight="SemiBold"
                Cursor="Hand" Margin="0,70,0,119" HorizontalAlignment="Center" Width="302"/>

        <TextBlock Grid.Row="10" HorizontalAlignment="Center" Margin="0,136,0,77">
            <Hyperlink Click="Hyperlink_Click">
                <Run Text="Esqueceu a senha ?"/>
            </Hyperlink>
        </TextBlock>
        <PasswordBox x:Name="TxtSenha" BorderBrush="#AAAAAA" BorderThickness="1" Margin="0,30,26,0" Grid.RowSpan="2" Grid.Row="7"/>
        <TextBlock
            Text="Senha" Margin="0,12,26,12" Foreground="#555555" Grid.Row="7"/>

    </Grid>
</Page>