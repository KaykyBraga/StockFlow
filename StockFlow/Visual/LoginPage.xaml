<Page x:Class="StockFlow.Visual.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="LoginPage">

    <Page.Resources>
        <Style x:Key="BaseInputStyle" TargetType="{x:Type Control}">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#AAAAAA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource BaseInputStyle}"/>
        <Style TargetType="{x:Type PasswordBox}" BasedOn="{StaticResource BaseInputStyle}"/>

        <Style x:Key="PrimaryActionButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1890E0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="302"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0368AB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="320">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Login" 
                   FontSize="28" 
                   FontWeight="SemiBold" 
                   HorizontalAlignment="Center" 
                   Foreground="#333333" Margin="0,0,0,25"/>

        <TextBlock Grid.Row="1" Text="Email:" Margin="0,0,0,5" Foreground="#555555"/>
        <TextBox x:Name="TxtUsuario" Grid.Row="2" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="3" Text="Senha" Margin="0,0,0,5" Foreground="#555555"/>

        <Grid Grid.Row="4" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <PasswordBox x:Name="TxtSenha" Grid.Column="0"/>            
        </Grid>

        <CheckBox Content="Mostrar Senha" Grid.Row="5" Margin="0,0,0,30" HorizontalAlignment="Left"/>

        <Button Grid.Row="7"
                Content="Entrar" 
                Style="{StaticResource PrimaryActionButtonStyle}" Click="Button_Click"/>
        <TextBlock Grid.Row="8" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Hyperlink Click="Hyperlink_Click">
                <Run Text="Esqueceu a senha ?"/>
            </Hyperlink>
        </TextBlock>

    </Grid>
</Page>