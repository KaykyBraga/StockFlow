<Window x:Class="StockFlow.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StockFlow"
        mc:Ignorable="d"
        Title="StockFlow - Menu" Height="720" Width="1280"
        MinHeight="720"
        MinWidth="1280"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
         
        Background="WhiteSmoke">

    <Window.Resources>
        <Color x:Key="ColorAccent" >#007ACC</Color>
        <Color x:Key="ColorHover" >#E6F2FF</Color>
        <Color x:Key="ColorText" >#333</Color>
        <Color x:Key="ColorExitRed" >#D32F2F</Color>
        <Color x:Key="ColorExitWhite" >White</Color>

        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource ColorAccent}"/>
        <SolidColorBrush x:Key="MenuHoverColor" Color="{StaticResource ColorHover}"/>
        <SolidColorBrush x:Key="MenuTextColor" Color="{StaticResource ColorText}"/>
        <SolidColorBrush x:Key="ExitRedColorBrush" Color="{StaticResource ColorExitRed}"/>
        <SolidColorBrush x:Key="ExitNormalBackground" Color="Transparent"/>
        <SolidColorBrush x:Key="SubMenuHoverBackground" Color="#FF5F5F5F"/>
        <SolidColorBrush x:Key="SubMenuHoverForeground" Color="{StaticResource ColorExitWhite}"/>

        <Style x:Key="SubMenuItemButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="{StaticResource MenuTextColor}"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" Padding="15,5,5,5" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource SubMenuHoverBackground}"/>
                                <Setter Property="Foreground" Value="{StaticResource SubMenuHoverForeground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Expander">
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="5,15,5,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Expander">
                        <DockPanel>
                            <ToggleButton DockPanel.Dock="Top" IsChecked="{Binding Path=IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" OverridesDefaultStyle="True" Content="{TemplateBinding Header}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Padding="10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0" ContentSource="Content" VerticalAlignment="Center"/>
                                                <Path x:Name="Arrow" Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" Stroke="#555" StrokeThickness="2" VerticalAlignment="Center" HorizontalAlignment="center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ExpandSite" DockPanel.Dock="Bottom" Visibility="Collapsed"/>
                        </DockPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ExitButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ExitNormalBackground}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource ExitRedColorBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1" Padding="10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Image Source="/Imagens/Armazem.png" Height="83" Margin="5,0,10,0" Width="99"/>
                <TextBlock Text="StockFlow" FontSize="40" FontWeight="Bold" Foreground="#FF020202" VerticalAlignment="Center" Margin="-12,0,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#FFE6E6E6" BorderBrush="#CCCCCC" BorderThickness="0,0,1,0">
                <DockPanel>

                    <Border DockPanel.Dock="Bottom" BorderBrush="#DDDDDD" BorderThickness="0,1,0,0" Padding="10">
                        <Button x:Name="BtnSair" Style="{StaticResource ExitButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <Path x:Name="ExitIcon" Fill="{StaticResource ExitRedColorBrush}" Width="20" Height="20" Margin="0,0,10,0" Data="M13 1.01L13 0H3C2.448 0 2 0.448 2 1v14c0 0.552 0.448 1 1 1h10c0.552 0 1-0.448 1-1V9.99h-1V15H3V1h10v0.01zM11 8H5V7h6V8zM14 7L11 4v2H5v1h6v2L14 7z"/>
                                <TextBlock x:Name="ExitText" Text="Sair" Foreground="{StaticResource ExitRedColorBrush}" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>
                            <Button.Triggers>
                                <EventTrigger RoutedEvent="MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="ExitText" Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" To="{StaticResource ColorExitWhite}" Duration="0:0:0.1"/>
                                            <ColorAnimation Storyboard.TargetName="ExitIcon" Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)" To="{StaticResource ColorExitWhite}" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="ExitText" Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" To="{StaticResource ColorExitRed}" Duration="0:0:0.1"/>
                                            <ColorAnimation Storyboard.TargetName="ExitIcon" Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)" To="{StaticResource ColorExitRed}" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Button.Triggers>
                        </Button>
                    </Border>

                    <StackPanel DockPanel.Dock="Top" Margin="10">
                        <TextBlock Text="Menu" FontSize="36" FontWeight="Bold" Margin="10" HorizontalAlignment="Center"/>

                        <Expander Header="Dashboard" FontSize="22" FontWeight="Bold" TextBlock.Foreground="Black">
                            <StackPanel Width="219">
                                <Button Name="btnResumoEstoque" Content="Resumo de Estoque" Style="{StaticResource SubMenuItemButtonStyle}" FontSize="18" Margin="0,0,0,0" Click="btnResumoEstoque_Click"/>
                                <Button Name="btnAlertas" Content="Alertas" Style="{StaticResource SubMenuItemButtonStyle}" FontSize="18" Margin="0,0,0,0" Click="btnAlertas_Click"/>
                            </StackPanel>
                        </Expander>

                        <Expander Header="Produtos" FontSize="22" FontWeight="Bold" TextBlock.Foreground="Black">
                            <StackPanel Margin="15,5,0,5">
                                <Button x:Name="btnListEstoque" Content="Listagem de Estoque" Style="{StaticResource SubMenuItemButtonStyle}" FontSize="18" Click="btnListEstoque_Click"/>
                                <Button x:Name="btnAddProduto" Content="Adicionar Produto" Style="{StaticResource SubMenuItemButtonStyle}" FontSize="18" Margin="0,0,0,0" Click="btnAddProduto_Click"/>
                                <Button x:Name="btnPromocoes" Content="Promoções" Style="{StaticResource SubMenuItemButtonStyle}" FontSize="18" Margin="0,0,0,0" Click="btnPromocoes_Click"/>
                            </StackPanel>
                        </Expander>

                        <Expander Header="Relatórios" FontSize="22" FontWeight="Bold" TextBlock.Foreground="Black">
                            <StackPanel Margin="15,5,0,5">
                                <Button Name="btnRelatVendas" Content="Vendas" Style="{StaticResource SubMenuItemButtonStyle}" FontSize="18" Margin="0,0,0,0" Click="btnRelatVendas_Click"/>
                                <Button Name="btnRelatMoviment" Content="Movimentação" Style="{StaticResource SubMenuItemButtonStyle}" FontSize="18" Margin="0,0,0,0" Click="btnRelatMoviment_Click"/>
                            </StackPanel>
                        </Expander>

                        <Expander Header="Funcionários" FontSize="22" FontWeight="Bold" TextBlock.Foreground="Black" Cursor="">
                            <StackPanel Width="219">
                                <Button x:Name="btnListFun" Content="Listagem de Funcionários" Style="{StaticResource SubMenuItemButtonStyle}" FontSize="18" Margin="-3,0,0,0" Click="btnListFun_Click"/>
                                <Button x:Name="btnAddFun" Content="Adicionar Funcionário" Style="{StaticResource SubMenuItemButtonStyle}" FontSize="18" Margin="-3,0,0,0" Width="218" Click="btnAddFun_Click"/>
                            </StackPanel>
                        </Expander>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Frame x:Name="ContentFrame" 
                   Grid.Column="1" 
                   Background="White"
                   NavigationUIVisibility="Hidden"/>

        </Grid>
    </Grid>
</Window>