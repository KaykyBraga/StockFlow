<Page x:Class="StockFlow.Visual.Produtos.CadastrarProduto"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:StockFlow"
      mc:Ignorable="d"
      Title="CadastroProdutoPage" Background="White">

    <Page.Resources>
        <Style x:Key="BlueActionButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#FF33ADFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF2182C3"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource BlueActionButtonStyle}">
            <Setter Property="Background" Value="#FF1890E0"/>
            <Setter Property="Height" Value="40"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF0368AB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="BackButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PopupTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#8C8C8C"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5" SnapsToDevicePixels="True">
                            <Grid>
                                <TextBlock Text="{TemplateBinding Tag}" Foreground="Gray" VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="5,0,5,0" IsHitTestVisible="False">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource AncestorType=TextBox}}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Margin="5,0,5,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="#7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="#1C86EE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FormTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0,0,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#8C8C8C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="#7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="#1C86EE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="White"/>
        <SolidColorBrush x:Key="SolidBorderBrush" Color="#8C8C8C"/>
        <SolidColorBrush x:Key="HighlightBrush" Color="#7EB4EA"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#1C86EE"/>
        <SolidColorBrush x:Key="GlyphBrush" Color="#333"/>

        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Border x:Name="BackgroundBorder" Grid.ColumnSpan="2" Background="Transparent"/>
                <Path x:Name="Arrow" Grid.Column="1" Fill="{StaticResource GlyphBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z"/>
            </Grid>
        </ControlTemplate>

        <ControlTemplate x:Key="ComboBoxTextBox" TargetType="TextBox">
            <Border x:Name="PART_ContentHost" Focusable="False" />
        </ControlTemplate>
        <Style x:Key="FormComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="5,0,0,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="{StaticResource SolidBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border x:Name="MainBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5" SnapsToDevicePixels="True">
                            <Grid>
                                <ToggleButton Name="ToggleButton" Template="{StaticResource ComboBoxToggleButton}" Grid.Column="2" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press"/>
                                <ContentPresenter Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="{TemplateBinding Padding}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                <TextBox x:Name="PART_EditableTextBox" Style="{x:Null}" Template="{StaticResource ComboBoxTextBox}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Margin="{TemplateBinding Padding}" Focusable="True" Background="Transparent" Visibility="Hidden" IsReadOnly="{TemplateBinding IsReadOnly}"/>
                                <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                    <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <Border x:Name="DropDownBorder" Background="{StaticResource WindowBackgroundBrush}" BorderThickness="1" BorderBrush="{StaticResource SolidBorderBrush}"/>
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsEditable" Value="true">
                                <Setter Property="IsTabStop" Value="false"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="{StaticResource HighlightBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                                <Setter TargetName="MainBorder" Property="Background" Value="#EEE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="PopupCancelButtonStyle" TargetType="Button" BasedOn="{StaticResource BlueActionButtonStyle}">
            <Setter Property="Background" Value="#FFAAAAAA"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF888888"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="SuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource BlueActionButtonStyle}">
            <Setter Property="Background" Value="#28a745"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ErrorButtonStyle" TargetType="Button" BasedOn="{StaticResource BlueActionButtonStyle}">
            <Setter Property="Background" Value="#dc3545"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#c82333"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Top" Margin="30,30,0,30">
                <Button Style="{StaticResource BackButtonStyle}" Click="BackButton_Click">
                    <ContentControl>
                        <Path Data="M10 0 L0 10 L10 20 Z" Fill="#555" Width="10" Height="20" Stretch="Fill"/>
                    </ContentControl>
                </Button>
                <TextBlock Text="Cadastro de Produto" FontSize="28" FontWeight="Bold" Foreground="#333" VerticalAlignment="Center" Margin="15,0,0,0"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="30,0,30,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" VerticalAlignment="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,15,10">
                        <TextBlock Text="Nome Produto" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtNomeProduto" Style="{StaticResource FormTextBoxStyle}" ToolTip="Nome Completo do Produto"/>
                    </StackPanel>
                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="15,0,0,10">
                        <TextBlock Text="Estoque Inicial" />
                        <TextBox x:Name="txtEstoqueInicial" Style="{StaticResource FormTextBoxStyle}" ToolTip="Ex.: 50"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,15,10">
                        <TextBlock Text="SKU (Código Interno)" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtSku" Style="{StaticResource FormTextBoxStyle}" ToolTip="Código único de identificação interna"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="15,0,0,10">
                        <TextBlock Text="EAN (Código de Barras)"/>
                        <TextBox x:Name="txtEan" Style="{StaticResource FormTextBoxStyle}" ToolTip="Código de 13 digitos. Ex.: 1111111111111"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,15,10">
                        <TextBlock Text="Preço de Custo" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtPrecodeCusto" Style="{StaticResource FormTextBoxStyle}" ToolTip="Ex.: R$ 10,00"/>
                    </StackPanel>
                    <StackPanel Grid.Row="2" Grid.Column="1" Margin="15,0,0,10">
                        <TextBlock Text="Estoque Mínimo"/>
                        <TextBox x:Name="txtEstoqueMinimo" Style="{StaticResource FormTextBoxStyle}" ToolTip="Ex.: 10"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,15,10">
                        <TextBlock Text="Preço de Venda" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtPrecodeVenda" Style="{StaticResource FormTextBoxStyle}" ToolTip="Ex.: R$ 50,00"/>
                    </StackPanel>
                    <StackPanel Grid.Row="3" Grid.Column="1" Margin="15,0,0,10">
                        <TextBlock Text="Localização no Estoque"/>
                        <TextBox x:Name="txtLocalizacao" Style="{StaticResource FormTextBoxStyle}" ToolTip="Ex: Prateleira 1, Corredor 1, Andar 1."/>
                    </StackPanel>

                    <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,10,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                <TextBlock Text="Marca" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbMarca" Style="{StaticResource FormComboBoxStyle}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="5,0,5,0">
                                <TextBlock Text="Fornecedor" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbFornecedor" Style="{StaticResource FormComboBoxStyle}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Margin="5,0,0,0">
                                <TextBlock Text="Categoria" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbCategoria" Style="{StaticResource FormComboBoxStyle}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                </Grid>

                <StackPanel Grid.Column="1" Margin="30,0,0,0" VerticalAlignment="Top">
                    <Button Content="Adicionar Marca" Style="{StaticResource BlueActionButtonStyle}" Margin="0,0,0,10" Click="btnAddMarca_Click"/>
                    <Button Content="Adicionar Fornecedor" Style="{StaticResource BlueActionButtonStyle}" Margin="0,0,0,10" Click="btnAddFornecedor_Click"/>
                    <Button Content="Adicionar Categoria" Style="{StaticResource BlueActionButtonStyle}" Margin="0,0,0,30" Click="btnAddCategoria_Click"/>

                    <Button Content="Cadastrar" Style="{StaticResource PrimaryButtonStyle}" Click="CadastrarButton_Click"/>
                </StackPanel>
            </Grid>
        </Grid>

        <Grid x:Name="PopupOverlay" Visibility="Collapsed">
            <Border Background="#80000000"/>

            <Border x:Name="PopupFornecedor" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Background="White" CornerRadius="8" BorderBrush="#CCCCCC" BorderThickness="1" Width="400">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="0" BlurRadius="10" Color="Black" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel Margin="25">
                    <TextBlock Text="Adicionar Fornecedor" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <TextBox x:Name="txtNomeFantasia" Style="{StaticResource PopupTextBoxStyle}" Tag="Nome Fantasia"/>
                    <TextBox x:Name="txtRazaoSocial" Style="{StaticResource PopupTextBoxStyle}" Tag="Razão Social"/>
                    <TextBox x:Name="txtCnpj" Style="{StaticResource PopupTextBoxStyle}" Tag="CNPJ (apenas números)" MaxLength="18" PreviewTextInput="NumberValidationTextBox" TextChanged="TxtCnpj_TextChanged"/>
                    <TextBox x:Name="txtEmail" Style="{StaticResource PopupTextBoxStyle}" Tag="Email"/>
                    <TextBox x:Name="txtTelefonePrincipal" Style="{StaticResource PopupTextBoxStyle}" Tag="Telefone (apenas números com DDD)" MaxLength="15" PreviewTextInput="NumberValidationTextBox" TextChanged="TxtTelefonePrincipal_TextChanged"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="btnAdicionarFornecedor" Grid.Column="0" Content="Adicionar" Style="{StaticResource BlueActionButtonStyle}" Margin="0,10,5,0" Click="btnAdicinarFornecedor_Click"/>
                        <Button Grid.Column="1" Content="Cancelar" Style="{StaticResource PopupCancelButtonStyle}" Margin="5,10,0,0" Click="btnCancelar_Click"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border x:Name="PopupMarca" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Background="White" CornerRadius="8" BorderBrush="#CCCCCC" BorderThickness="1" Width="350">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="0" BlurRadius="10" Color="Black" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel Margin="25">
                    <TextBlock Text="Adicionar Marca" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <TextBox x:Name="txtNomeMarca" Style="{StaticResource PopupTextBoxStyle}" Tag="Nome da Marca"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="btnAdicionarMarca" Grid.Column="0" Content="Adicionar" Style="{StaticResource BlueActionButtonStyle}" Margin="0,10,5,0" Click="btnAdicinarMarca_Click"/>
                        <Button Grid.Column="1" Content="Cancelar" Style="{StaticResource PopupCancelButtonStyle}" Margin="5,10,0,0" Click="btnCancelar_Click"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border x:Name="PopupCategoria" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Background="White" CornerRadius="8" BorderBrush="#CCCCCC" BorderThickness="1" Width="350">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="0" BlurRadius="10" Color="Black" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel Margin="25">
                    <TextBlock  Text="Adicionar Categoria" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <TextBox x:Name="txtNomeCategoria" Style="{StaticResource PopupTextBoxStyle}" Tag="Nome da Categoria"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="btnAdicionar" Grid.Column="0" Content="Adicionar" Style="{StaticResource BlueActionButtonStyle}" Margin="0,10,5,0" Click="btnAdicinarCategoria_Click"/>
                        <Button Grid.Column="1" Content="Cancelar" Style="{StaticResource PopupCancelButtonStyle}" Margin="5,10,0,0" Click="btnCancelar_Click"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border x:Name="PopupSucesso" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#F0FFF4" CornerRadius="8" BorderBrush="#28a745" BorderThickness="1" Width="350">
                <StackPanel Margin="25">
                    <Border Width="60" Height="60" CornerRadius="30" Background="#28a745" HorizontalAlignment="Center">
                        <TextBlock Text="✓" Foreground="White" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,5"/>
                    </Border>
                    <TextBlock x:Name="PopupMensagemSucesso" Text="Cadastro realizado com sucesso!" FontSize="16" TextWrapping="Wrap" TextAlignment="Center" Margin="0,15,0,20"/>
                    <Button Content="OK" Style="{StaticResource SuccessButtonStyle}" Width="100" Click="btnPopupOk_Click"/>
                </StackPanel>
            </Border>

            <Border x:Name="PopupErro" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#FFF0F1" CornerRadius="8" BorderBrush="#dc3545" BorderThickness="1" Width="350">
                <StackPanel Margin="25">
                    <Border Width="60" Height="60" CornerRadius="30" Background="#dc3545" HorizontalAlignment="Center">
                        <TextBlock Text="!" Foreground="White" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,5"/>
                    </Border>
                    <TextBlock x:Name="PopupMensagemErro" Text="Ocorreu um erro." FontSize="16" TextWrapping="Wrap" TextAlignment="Center" Margin="0,15,0,20"/>
                    <Button Content="OK" Style="{StaticResource ErrorButtonStyle}" Width="100" Click="btnPopupOk_Click"/>
                </StackPanel>
            </Border>

        </Grid>

    </Grid>
</Page>