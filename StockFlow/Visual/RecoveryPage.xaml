<Page x:Class="StockFlow.Visual.RecoveryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
        <TextBlock Text="Recuperação de Senha" 
                   FontSize="24" 
                   FontWeight="SemiBold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,30" 
                   Foreground="#333333"/>

        <TextBlock Text="Digite seu e-mail:" Margin="0,0,0,5" Foreground="#555555"/>
        <TextBox Height="35" BorderBrush="#AAAAAA" BorderThickness="1" Margin="0,0,0,20"/>

        <Button Content="Enviar Link" 
                Height="40" 
                Background="#FF9999" 
                BorderBrush="#FF6666" 
                BorderThickness="1"
                Foreground="White"
                FontSize="16"
                FontWeight="SemiBold"
                Margin="0,0,0,20"/>

        <TextBlock HorizontalAlignment="Center">
            <Hyperlink Click="Hyperlink_Login_Click">
                <Run Text="&lt; Voltar para o Login"/>
            </Hyperlink>
        </TextBlock>
    </StackPanel>
</Page>